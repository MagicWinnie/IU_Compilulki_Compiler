// Conditional statement test
// "Complex way" of two reals comparison

Program: Main(1.1, 1.2)

class Main is
    this(a: Real, b: Real) is

        var eq : true
        var dif : false

        if a.Equal(b).Not() then
            dif.print()
        else
            eq.print()
        end

    end

end